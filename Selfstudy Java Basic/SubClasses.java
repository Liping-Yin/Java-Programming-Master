package selfstudy;
/**
 * Using the protected modifier is strictly more liberal than using no modifier at all: 
 * It allows access from classes in the same package and from subclasses that are not in the same package.
 * 
 * A subclass can add to the structure and behavior that it inherits. 
 * It can also replace or modify inherited behavior (though not inherited structure).
 * @author Gebruiker
 *
 */
public class SubClasses {

}
